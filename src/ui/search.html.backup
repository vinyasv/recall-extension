<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Memex - Search Your History</title>
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      min-height: 100vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      padding: 2rem;
    }

    .container {
      max-width: 900px;
      width: 100%;
    }

    .header {
      text-align: center;
      margin-bottom: 2rem;
    }

    .logo {
      color: white;
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 0.5rem;
    }

    .tagline {
      color: rgba(255, 255, 255, 0.9);
      font-size: 1.2rem;
      font-weight: 300;
    }

    .stats-bar {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(10px);
      border-radius: 8px;
      padding: 0.75rem 1.5rem;
      margin-bottom: 1.5rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      color: white;
      font-size: 0.9rem;
    }

    .stat-item {
      display: flex;
      align-items: center;
      gap: 0.5rem;
    }

    .stat-value {
      font-weight: 600;
    }

    .search-box {
      background: white;
      border-radius: 12px;
      padding: 1.5rem;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.2);
      margin-bottom: 2rem;
      position: relative;
    }

    .search-input-wrapper {
      position: relative;
      display: flex;
      align-items: center;
    }

    .search-input {
      width: 100%;
      padding: 1rem 3rem 1rem 1rem;
      font-size: 1.1rem;
      border: 2px solid #e0e0e0;
      border-radius: 8px;
      outline: none;
      transition: border-color 0.3s;
    }

    .search-input:focus {
      border-color: #667eea;
    }

    .clear-button {
      position: absolute;
      right: 0.75rem;
      background: none;
      border: none;
      color: #999;
      cursor: pointer;
      font-size: 1.5rem;
      padding: 0.25rem 0.5rem;
      display: none;
      transition: color 0.2s;
    }

    .clear-button:hover {
      color: #667eea;
    }

    .clear-button.show {
      display: block;
    }

    .search-hint {
      font-size: 0.85rem;
      color: #999;
      margin-top: 0.5rem;
      text-align: center;
    }

    .results-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 1rem;
      color: white;
      font-size: 0.9rem;
    }

    .results-count {
      font-weight: 600;
    }

    .search-time {
      opacity: 0.8;
    }

    .results {
      display: none;
    }

    .results.show {
      display: block;
    }

    .result-card {
      background: white;
      border-radius: 12px;
      padding: 1.5rem;
      margin-bottom: 1rem;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      cursor: pointer;
      transition: transform 0.2s, box-shadow 0.2s;
    }

    .result-card:hover {
      transform: translateY(-2px);
      box-shadow: 0 6px 20px rgba(0, 0, 0, 0.15);
    }

    .result-header {
      display: flex;
      justify-content: space-between;
      align-items: flex-start;
      margin-bottom: 0.5rem;
    }

    .result-title {
      font-size: 1.2rem;
      font-weight: 600;
      color: #333;
      flex: 1;
    }

    .similarity-badge {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      padding: 0.25rem 0.75rem;
      border-radius: 20px;
      font-size: 0.85rem;
      font-weight: 600;
      margin-left: 1rem;
    }

    .result-url {
      font-size: 0.9rem;
      color: #667eea;
      margin-bottom: 0.75rem;
      word-break: break-all;
    }

    .result-summary {
      color: #666;
      line-height: 1.6;
      margin-bottom: 0.75rem;
    }

    .result-metadata {
      display: flex;
      gap: 1.5rem;
      font-size: 0.85rem;
      color: #999;
      padding-top: 0.75rem;
      border-top: 1px solid #f0f0f0;
    }

    .metadata-item {
      display: flex;
      align-items: center;
      gap: 0.25rem;
    }

    .empty-state {
      text-align: center;
      color: rgba(255, 255, 255, 0.9);
      padding: 3rem;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border-radius: 12px;
    }

    .empty-state-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .empty-state-title {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .empty-state-text {
      font-size: 1rem;
      opacity: 0.8;
    }

    .no-db-state {
      text-align: center;
      color: rgba(255, 255, 255, 0.9);
      padding: 3rem;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      border-radius: 12px;
    }

    .no-db-state-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .no-db-state-title {
      font-size: 1.3rem;
      font-weight: 600;
      margin-bottom: 0.5rem;
    }

    .no-db-state-text {
      font-size: 1rem;
      opacity: 0.8;
      line-height: 1.6;
    }

    .loading-skeleton {
      background: white;
      border-radius: 12px;
      padding: 1.5rem;
      margin-bottom: 1rem;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .skeleton-line {
      height: 20px;
      background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
      background-size: 200% 100%;
      animation: loading 1.5s infinite;
      border-radius: 4px;
      margin-bottom: 0.75rem;
    }

    .skeleton-line.title {
      width: 70%;
      height: 24px;
    }

    .skeleton-line.url {
      width: 50%;
      height: 16px;
    }

    .skeleton-line.summary {
      width: 100%;
    }

    @keyframes loading {
      0% {
        background-position: 200% 0;
      }
      100% {
        background-position: -200% 0;
      }
    }

    .privacy-note {
      text-align: center;
      color: rgba(255, 255, 255, 0.7);
      font-size: 0.9rem;
      margin-top: 3rem;
    }

    .error-state {
      background: white;
      border-radius: 12px;
      padding: 2rem;
      text-align: center;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    }

    .error-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
    }

    .error-title {
      font-size: 1.2rem;
      font-weight: 600;
      color: #e53e3e;
      margin-bottom: 0.5rem;
    }

    .error-message {
      color: #666;
      margin-bottom: 1rem;
    }

    .retry-button {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
      border: none;
      padding: 0.75rem 2rem;
      border-radius: 8px;
      font-size: 1rem;
      cursor: pointer;
      transition: transform 0.2s;
    }

    .retry-button:hover {
      transform: translateY(-2px);
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <div class="logo">Memex</div>
      <div class="tagline">Search what you meant, not what you typed</div>
    </div>

    <div class="stats-bar" id="statsBar">
      <div class="stat-item">
        <span>📚</span>
        <span><span class="stat-value" id="totalPages">0</span> pages indexed</span>
      </div>
      <div class="stat-item">
        <span>⚡</span>
        <span id="status">Ready</span>
      </div>
    </div>

    <div class="search-box">
      <div class="search-input-wrapper">
        <input
          type="text"
          class="search-input"
          placeholder="Search your browsing history semantically..."
          id="searchInput"
        >
        <button class="clear-button" id="clearButton">×</button>
      </div>
      <div class="search-hint">
        Try: "articles about async programming" or "machine learning tutorials"
      </div>
    </div>

    <div id="resultsHeader" class="results-header" style="display: none;">
      <span class="results-count" id="resultsCount"></span>
      <span class="search-time" id="searchTime"></span>
    </div>

    <div id="results" class="results">
      <!-- Results will be inserted here -->
    </div>

    <div id="emptyState" class="empty-state">
      <div class="empty-state-icon">🔍</div>
      <div class="empty-state-title">Start Searching</div>
      <div class="empty-state-text">Type a query above to search your browsing history semantically</div>
    </div>

    <div id="noDbState" class="no-db-state" style="display: none;">
      <div class="no-db-state-icon">📭</div>
      <div class="no-db-state-title">No Pages Indexed Yet</div>
      <div class="no-db-state-text">
        Browse the web for a while, and Memex will automatically index pages you spend time on.<br>
        Come back later to search your browsing history!
      </div>
    </div>

    <div class="privacy-note">
      🔒 All data stays on your device. Nothing is sent to external servers.
    </div>
  </div>

  <script type="module" src="./search.ts"></script>
</body>
</html>
